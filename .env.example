ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (local defaults shown)
# DB_PATH=data/news.db
# LOG_LEVEL=INFO
# SCAN_INTERVAL_HOURS=24
# MIN_RELEVANCE_SCORE=5
# LLM_MODEL=claude-sonnet-4-6
HOST=127.0.0.1
PORT=8080
BACKUP_TOKEN=          # leave empty locally; set a secret for cloud

# ── Railway dashboard variables (do NOT put in .env / git) ──────────────────
# HOST=0.0.0.0
# PORT=<injected automatically by Railway — do not set>
# DB_PATH=/data/news.db
# BACKUP_TOKEN=<generate with: openssl rand -hex 32>
# ANTHROPIC_API_KEY=<your real key>
